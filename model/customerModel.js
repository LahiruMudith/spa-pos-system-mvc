export default class CustomerModel {
    constructor(cId, firstName, lastName, phoneNumber, address) {
        this.cId = cId;
        this.firstName = firstName;
        this.lastName = lastName;
        this.phoneNumber = phoneNumber;
        this.address = address;
    }
}